exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Got the form by get! using terraform'),
    };
    return response;
};